{
    "name": "frame-bridge-monorepo",
    "private": true,
    "workspaces": ["packages/*"],
    "scripts": {
        "build": "npm run build -w packages/frame-bridge && npm run build -w packages/react-frame-bridge && npm run build -w packages/react-frame-bridge-devtools",
        "test": "npm test -w packages/frame-bridge",
        "typecheck": "npm run typecheck --workspaces --if-present"
    }
}
